import React from 'react';

const FriendCard = ({friend}) => {
    
    const {fullName} = friend

    return (
        <div>
            Friend Card
        </div>
    );
};

export default FriendCard;